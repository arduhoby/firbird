class AppConfig {
  /// Application version number (Single Source of Truth)
  static const String appVersion = '0.5.0';

  /// Application build number
  static const String buildNumber = '50';

  /// Formatted version string (e.g. 'v0.4.1')
  static const String fullVersion = 'v$appVersion';
}
