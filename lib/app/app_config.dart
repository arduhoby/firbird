class AppConfig {
  /// Application version number (Single Source of Truth)
  static const String appVersion = '0.3.2';

  /// Application build number
  static const String buildNumber = '32';

  /// Formatted version string (e.g. 'v0.3.2')
  static const String fullVersion = 'v$appVersion';
}
